{
  "name": "sub",
  "version": "1.0.0",
  "scripts": {
    "build": "tsup",
    "lint": "eslint --cache 'src/**/*.ts' 'src/*.ts' --max-warnings 0",
    "format": "prettier --cache --write 'src/**/*.ts' 'src/*.ts'"
  },
  "dependencies": {
    "main": "*"
  },
  "devDependencies": {
  }
}
